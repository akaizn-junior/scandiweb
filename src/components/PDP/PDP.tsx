import React from 'react';
import './PDP.css';

class PDP extends React.Component {
  render() {
    return (
        <div className="PDP">
            HELLO
        </div>
    );
  }
}

export default PDP;
